import "package:flutter/material.dart";

class MyGoalsPage extends StatelessWidget {
  const MyGoalsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("A preview for my goals page"),
    );
  }
}
